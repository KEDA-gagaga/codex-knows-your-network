interface:
  display_name: "Network State"
  short_description: "维护、强制同步并校验已初始化的私人网络状态与排错记录"
  default_prompt: "Use $network-state to synchronize first, then query or update my initialized private network-state records."
